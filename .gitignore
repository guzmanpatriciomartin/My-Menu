node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Runtime JSON datastore (contains order data / customer comments)
db_temp.json

# Firebase service-account keys — NEVER commit these
*serviceaccount*.json
service-account*.json
service_account*.json
*-firebase-adminsdk-*.json
firebase-service-account*.json

# curl cookie jars — contain live mimenu_session JWTs
cookies.txt
*.cookies
cookiejar*

# Working plan, kept local on purpose
docs/PLAN_V1.5.md
